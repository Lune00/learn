declare
  -- declaration d'une variable de même type que la nom
  v_nom  clients.nomcli%type;
  -- declaration de 2 variables de même type
  x number(10,3);
  y x%type
begin
  -- instructions
end;
