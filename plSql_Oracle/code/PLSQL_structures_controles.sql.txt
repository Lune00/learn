--IF
--
--DECLARE
--nombreEmployes INT;
--BEGIN
--SELECT COUNT(*) INTO nombreEmployes FROM emp;
--IF nombreEmployes < 10 THEN
--    DBMS_OUTPUT.put_line('Embaucher');
--ELSE
--    DBMS_OUTPUT.put_line('Trop d''employés');
--END IF;
--END;
--/

--CASE

--DECLARE
--
--salaire emp.sal%type;
--
--BEGIN
--
--SELECT sal INTO salaire FROM emp WHERE ename='JONES';
--
--CASE
--    WHEN salaire < 1000 THEN 
--    DBMS_OUTPUT.put_line('Augmenter');
--    WHEN salaire < 3000 THEN
--    DBMS_OUTPUT.put_line('Stable');
--    ELSE
--    DBMS_OUTPUT.put_line('Trop payé');
--END CASE;
--
--END;
--/

--LOOP

DROP TABLE t10;
CREATE TABLE t10 (code NUMBER(10), nom varchar2(20));
--TRUNCATE TABLE t10;

DECLARE

nombreEnregistrements INT :=0;

BEGIN

LOOP
nombreenregistrements := nombreenregistrements + 1;
INSERT INTO t10 VALUES(nombreenregistrements,'Nom'||nombreenregistrements);
EXIT WHEN nombreenregistrements = 5;

END LOOP;
END;
/
SELECT * FROM t10;

--FOR :
TRUNCATE TABLE t10;
BEGIN
FOR n IN 1..5 LOOP

 INSERT INTO t10 VALUES(n,'Nom'||n);
 --DBMS_OUTPUT.put_line(n);
END LOOP;
COMMIT;
END;
/

SELECT * FROM t10;

--WHILE

DECLARE
x INT:=0;
BEGIN

WHILE x <> 5 LOOP

 DBMS_OUTPUT.put_line(x);
 x:= x + 1 ;
END LOOP;
END;
/